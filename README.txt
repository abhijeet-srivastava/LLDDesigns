## Project Overview
LowLevel Designs of production ready Java libraries and Utility classes.
Each library is defined as separate module.
Each library is independent component and has its own test suite validations.
Java 17 | Maven 3.8.7